# 02 — Privacy Filter — Tasks

- [ ] **T-02.1** Implement `PrivacyFilter` with `defaultSkipTypes` and injectable
  `skipTypes`. _(02-AC-1, 02-AC-5)_
- [ ] **T-02.2** Implement `shouldSkip(types:)` as pure set-intersection logic.
  _(02-AC-2, 02-AC-3, 02-AC-5)_
- [ ] **T-02.3** Wire the filter into `ClipboardMonitor.poll()` before any content read.
  _(02-AC-4)_

## Tests (PastaEngineTests / PrivacyFilterTests)

- [ ] **TT-02.a** `shouldSkip` returns true for each of ConcealedType, TransientType,
  AutoGeneratedType. _(02-AC-1, 02-AC-2)_
- [ ] **TT-02.b** `shouldSkip` returns false for ordinary types
  (`public.utf8-plain-text`, `public.rtf`). _(02-AC-3)_
- [ ] **TT-02.c** Mixed types (skip + ordinary) → `shouldSkip` returns true. _(02-AC-2)_
- [ ] **TT-02.d** Monitor integration: a fake pasteboard reporting ConcealedType produces
  no `onCapture`, and `string(forType:)`/`data(forType:)` are never called (verify via a
  recording fake). _(02-AC-4)_

## Verification

`make test` passes `PrivacyFilterTests`. Manual: copy a password from a password manager
(e.g. 1Password) and confirm it does NOT appear in the history window.
